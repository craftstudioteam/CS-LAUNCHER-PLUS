# PHASE 9 PLAN — root causes + chunks

Baseline: `c5cbe80` (phase 8, CI green). Rule: keep every legacy view id / view type, replace
structure not just palette, push after each chunk, WAIT for CI.

## Root-cause table

| # | Request | Where | Root cause / design | Fix |
|---|---------|-------|---------------------|-----|
| 1 | Loader pick (e.g. Fabric) must auto-fill loader version | `VersionCreateFragment.onVersionPicked` (MC pick) + `applyLoaderSelection` | After an MC pick the code deliberately sets `mLoaderVersion=null` and waits for a manual pick. | New `autoSelectLoaderBuild(loader, mc)`: bg thread → Fabric/Quilt `downloadLoaderVersions(mc)` first `stable` (fallback first); Forge `downloadForgeVersions()` first build with prefix `mc-` (list is newest-last → pick last matching); NeoForge first build whose `neoforgeToMcVersion==mc` (pick last = newest). Shows "…" while resolving, field text "0.16.9 · auto" (tap still opens picker). Also runs when a loader chip is picked while MC already chosen (`applyLoaderSelection(index, keepMc=true)`). Guarded by a request token so stale results never overwrite a manual pick. |
| 2 | Settings: every page new design | `fragment_custom_settings.xml`, `item_settings_category.xml`, `item_setting_toggle/slider/dropdown/button/option.xml`, `item_setting_category_card.xml`, `item_settings_nav_chip.xml`, `item_settings_dashboard_stat.xml`, `item_settings_pin_card.xml` | Current = header + horizontal chip rail + card rows (v "GX"). | New "Console" structure: LEFT vertical section rail (icon + label column, `settings_category_rail` LinearLayout kept but vertical inside a vertical ScrollView `settings_category_rail_scroll`) + RIGHT content column with compact title bar (back, icon, title, subtitle, badge) and a full-width search bar ABOVE the list. Rows become bordered "spec sheet" lines: left 2dp status rail, no icon wells (icon small inline), value on the right, dividers instead of floating cards; sliders get a value pill + track; dropdown shows value chip with chevron; dashboard cards become 2-col "tiles" with big glyph + count line. Save bar becomes bottom sticky "COMMIT" strip. All ids + types preserved (rail stays `LinearLayout`, scroll view type changes from HorizontalScrollView → ScrollView: code holds it as `View`, OK). |
| 3 | Modpack browse new design | `fragment_mod_search.xml` (SearchModFragment) + `item_mod_modern.xml` (shared with mods grid) | Plain header + list. | New "storefront": hero header (kicker MODPACKS, big title, source pills), search capsule + filter, sticky status, results list. Card redesign: banner with gradient scrim, icon docked bottom-left over banner, title/author, stats row (downloads/followers), tags row, INSTALL pill right; card root LinearLayout + all ids kept. Skeleton shimmer stays in DownloadListFragment. |
| 4 | Boot log too small | `BootLogOverlay` 300dp / 8.5sp / 9 lines | constants | 420dp (or 46% of width, whichever smaller), 11sp lines, 12 lines, header 10sp, hairline spacing, bigger EXPAND pill. |
| 5 | Premium skin not on home 3D / head | `SkinResolver.resolve` MS branch → `ShortcutSkinHeadHelper.getFullSkinFile` → `DownloadUtils.downloadFile(skinUrl)` with `http://textures.minecraft.net/...` (session server returns http URL) → blocked by cleartext policy (targetSdk 34) → fails silently → falls to mc-heads (also may fail with default UA) → Steve. Also `MicrosoftBackgroundLogin` only calls `updateSkinFace()` (head only via mc-heads). | `getSkinUrl` → force `https://`; `getFullSkinFile` downloads via URLConnection with UA + validates PNG≥64×32. MS login: after profile → `SkinResolver.resolve(acc)` + `updateSkinFace` writes head from the full skin; `LauncherHomeFragment.loadPlayerSkin` bumps `SkinHead3DRenderer.invalidate(name)` + reloads head after a successful resolve so the 3D head updates too. |
| 6 | Force close → land in launcher | `Tools.dialogForceClose` → `fullyExit()` kills :game process; launcher process was killed at launch (`ContextAwareDoneListener` kills itself) so nothing is left to show. | New `Tools.quitToLauncher(Activity)`: start `LauncherActivity` with NEW_TASK|CLEAR_TASK (the :launcher process restarts), then kill the game process 250ms later. Custom confirm card (CsConfirmDialog style) replaces the stock AlertDialog. Same path used by the drawer item 0. |
| 7 | In-game drawer buttons + Quick Settings redesign w/ animation | `item_ingame_menu_tile.xml` (TextView root contract), `activity_basemain.xml` drawer ListView, `dialog_side_dialog.xml`, `dialog_quick_setting.xml`, `SideDialogView` | Plain tiles; side dialog = grey card; quick settings = constraint soup. | Drawer: `navDrawer` ListView gets a custom adapter (`InGameMenuAdapter`, icon + title + subtitle + accent for QUIT), staggered slide-in on drawer open (`DrawerLayout.addDrawerListener`), item press pop. Side dialog frame: header with glyph + title + close pill, rounded 22dp graphite sheet with left accent rail, entrance uses scale .96→1 + fade in addition to slide. Quick settings content: sectioned (DISPLAY / MOTION / INPUT / HUD) with row cards, every id/type kept (`Switch`, `com.kdt.CustomSeekbar`, TextViews). |
| 8 | Profile edit: Java + Renderer popups | `ProfileEditorFragment` spinners `vprof_editor_spinner_runtime`, `vprof_editor_profile_renderer` | Stock Spinner dropdowns. | Keep the Spinners (state holders, code reads `getSelectedItem/Position`) but make them non-interactive (`setEnabled(false)`, `setClickable(false)`), and put a tap overlay button on each field opening `CsPickerSheet` (new generic Dialog: title, subtitle, list rows with icon/mono meta/selected check, footer). Selection → `spinner.setSelection(i)` so all existing save logic works. Runtime rows show Java version + arch + name; renderer rows show display name + id + hint. |
| 9 | Default skin swap | `res/drawable/ic_steve.png` used in 4 java + 1 xml | Steve | Downloaded official "Sunny" (classic 64×64, minecraft.wiki) → `res/drawable/cs_default_skin.png`; update refs; delete `ic_steve.png`. |
| 10 | Login pages new design | `fragment_select_auth_method.xml`, `fragment_microsoft_login.xml`, `fragment_elyby_login.xml`, `fragment_elyby_browser.xml`, `fragment_local_login.xml` | Card stacks. | Auth select: split "gateway" (left brand panel with 3D-ish tiles, right list of methods as horizontal rows w/ numbered rail). Microsoft: full-bleed WebView with slim floating status strip (progress line + secure badge). Ely.by login: two-column form (left: identity fields with floating labels, right: "what you get" + create-account CTA), inline error banner instead of Toast/dialog, loading state animation. Ely browser: chrome bar w/ url pill, progress. Local: keep phase-8 v2 structure but restyle to the new "console" language (small refinements). |
| 11 | Remove Show FPS / Transparent FPS in settings; FPS element gets own customization | `LauncherPreferenceFragment` perf items; `EditControlSideDialog.loadValues` | Prefs drive the legacy chip HUD. | Remove the 2 items (chip logic stays inert = pref false). FPS control editor: new "FPS READ-OUT" group in `dialog_control_button_setting.xml` — Transparent background toggle (sets bgColor alpha 0 + strokeWidth 0 → stored in existing ControlData fields, so zero schema change), text size slider (new optional `ControlData.fpsTextSize`, default 0=auto, serialised by Gson, old layouts unaffected), show unit toggle (`fpsShowUnit`, default true). ControlFps reads both. |
| 12 | Color picker opens on the same side as edit panel | `EditControlSideDialog` calls `appearColor(isAtRight(), …)`; `SideDialogView.isAtRight()` reports the EDIT panel's side → picker slides in on the same side and covers it. | Pass `!isAtRight()` (opposite side) in all 3 call sites (bg, stroke, adaptPanelPosition). |
| 13 | About page new design | `fragment_about.xml` (807 lines) + `AboutFragment` | Card stack. | New "editorial" layout: left sticky identity column (logo, name, version chip, links as icon tiles) + right scroll (story paragraphs w/ numbered chapters, credits as a two-row ledger, legal in mono block). All ids preserved. |

## Chunks (each: parse-scan + resource check → commit → push → CI wait)

- **A** (logic + fixes): 1, 4, 5, 6, 9, 11 (settings removal + FPS control group), 12. Boot log size.
- **B** (in-game UI): 7 drawer adapter + side dialog frame + quick settings layout; 8 pickers + CsPickerSheet.
- **C** (pages): 2 settings redesign, 3 modpack browse, 10 login pages, 13 about.

Palette: graphite only (see phase 8). No green.
