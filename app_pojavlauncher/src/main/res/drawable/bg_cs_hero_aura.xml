<?xml version="1.0" encoding="utf-8"?>
<!-- HERO AURA — brand violet glow bleeding down the hero card (the
     "client features" identity color, used sparingly as ambient light). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="20dp" />
    <gradient
        android:angle="270"
        android:startColor="#2ED0D0D0"
        android:centerColor="#100B0B0E"
        android:endColor="#000B0B0E" />
</shape>
