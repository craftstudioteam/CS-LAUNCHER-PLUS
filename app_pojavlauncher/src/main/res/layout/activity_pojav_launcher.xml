<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/bgMainDrawable">

    <com.kdt.mcgui.mcAccountSpinner
        android:id="@+id/account_spinner"
        android:layout_width="match_parent"
        android:layout_height="@dimen/_52sdp"
        android:dropDownWidth="280dp"
        android:dropDownVerticalOffset="@dimen/_52sdp"
        android:popupBackground="@drawable/bg_account_popup"
        android:popupElevation="10dp"
        app:layout_constraintTop_toTopOf="parent" />
    <ImageButton
        android:id="@+id/setting_button"
        android:layout_width="@dimen/_52sdp"
        android:layout_height="@dimen/_52sdp"
        android:background="?attr/selectableItemBackground"
        android:src="@drawable/ic_menu_settings"
        android:scaleType="fitCenter"
        android:paddingVertical="@dimen/_8sdp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Persistent left navigation (shared across every launcher page) -->
    <include
        android:id="@+id/lh_dock"
        layout="@layout/include_lh_dock"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="6dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/account_spinner" />

    <!-- Holding most of the dynamic content -->
    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/container_fragment"
        android:layout_width="0dp"
        app:layout_constraintStart_toEndOf="@+id/lh_dock"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_height="0dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/account_spinner" />

    <!-- Old on-screen download progress bar REMOVED (developer req 8). Download
         status is now surfaced only by the notification system. The download
         engine (ProgressKeeper/DownloadControl) is untouched (req 9). -->

    <!-- Shortcut boot overlay: identical to the shortcut's "Opening Game…"
         screen so the hand-off never flashes the launcher home UI -->
    <include
        android:id="@+id/launch_boot_overlay"
        layout="@layout/screen_opening_game"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone" />

    <!-- Cosmetic cold/warm start greeting. It never blocks initialization or touch. -->
    <LinearLayout
        android:id="@+id/welcome_back_overlay"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:alpha="0"
        android:background="@drawable/bg_skin_library_panel"
        android:clickable="false"
        android:elevation="16dp"
        android:gravity="center"
        android:orientation="vertical"
        android:paddingHorizontal="30dp"
        android:paddingVertical="18dp"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">
        <ImageView android:layout_width="38dp" android:layout_height="38dp" android:contentDescription="@null" android:src="@mipmap/ic_launcher_round" />
        <TextView android:layout_width="wrap_content" android:layout_height="wrap_content" android:layout_marginTop="8dp" android:letterSpacing="0.16" android:text="WELCOME BACK" android:textColor="#FFFFFF" android:textSize="16sp" android:textStyle="bold" />
        <TextView android:layout_width="wrap_content" android:layout_height="wrap_content" android:layout_marginTop="5dp" android:text="Your profiles are ready" android:textColor="#A0A0A4" android:textSize="9sp" />
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>